# Skill Registry — drop-one-product

Generated by sdd-init. Source: `~/.claude/skills/*` (user-level, mirrored in opencode/gemini/cursor dirs) + `~/.claude/CLAUDE.md` (global user instructions, no project-level CLAUDE.md exists — greenfield).

## Compact Rules (inject into sub-agent prompts as "Project Standards (auto-resolved)")

- **Commits**: Conventional commits only. NEVER add "Co-Authored-By" or AI attribution to commit messages.
- **Build**: NEVER run a build after making changes (no `npm run build` / `astro build` as a verification step) unless explicitly asked.
- **CLI tools**: Use `bat`/`rg`/`fd`/`sd`/`eza` instead of `cat`/`grep`/`find`/`sed`/`ls`. Install via brew if missing.
- **Verification**: Never agree with a technical claim without checking code/docs first.
- **Questions**: When asking the user something, stop and wait — never assume or continue past a question.
- **Alternatives**: Propose alternatives with tradeoffs when architecturally relevant (not on every message).

## User Skills — Trigger Table

| Skill | Trigger | Relevant here? |
|-------|---------|-----------------|
| go-testing | Writing Go tests, Bubbletea TUI testing | No (project is Astro/TS, no Go) |
| skill-creator | User asks to create a new skill / document AI patterns | No |
| branch-pr | Creating a pull request, preparing a branch for review | Only if/when a PR is requested |
| judgment-day | User says "judgment day", "dual review", "juzgar" | Only if user invokes it |
| issue-creation | Creating a GitHub issue (bug/feature) | Only if user requests it |

## SDD Skills (excluded from compact-rule injection — orchestrator-only)
sdd-init, sdd-explore, sdd-propose, sdd-spec, sdd-design, sdd-tasks, sdd-apply, sdd-verify, sdd-archive, sdd-onboard, skill-registry, _shared

## Project Conventions Detected
- No project-level CLAUDE.md / AGENTS.md / .cursorrules found (greenfield, empty directory).
- Global user CLAUDE.md applies (rules summarized above).
