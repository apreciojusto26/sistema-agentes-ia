/**
 * The social preview image, by filename in public/ — or null when this product
 * has none.
 *
 * A GENERATED VALUE. It used to be the literal '/og-cover.png' inside
 * Base.astro, resolved against a hardcoded domain, so every landing produced
 * from this template offered AstraVibe's own artwork on someone else's host.
 *
 * The generator overwrites this module with the product's own main image and
 * keeps its real extension. `null` is a real answer: absence is honest, and a
 * competitor's product is not.
 */
export const ogImageFile: string | null = 'og-cover.png';
