---
// LEGACY PATH SHIM — kept deliberately, not dead code.
//
// The composition this file used to hold moved to
// @/design-system/blocks/social-proof/ReviewsReel/Carousel.astro when
// socialProof/ReviewsReel gained real structural variants (carousel | grid).
// The registry now points at the block; nothing resolves this path through
// the registry any more.
//
// It survives because design-system/test-fixtures/LegacyIndex2074c93.astro is
// BYTE-LOCKED to index.astro at HEAD 2074c93 and imports this module
// statically. legacy-render.golden.test.ts renders that fixture against the
// live default-DesignSpec page and requires the two to be byte-identical, so
// this shim is what keeps that proof honest: the legacy import path and the
// registry path must reach the SAME composition, and here they demonstrably do.
//
// Do not re-add markup here. Edit the block.
import Carousel from '@/design-system/blocks/social-proof/ReviewsReel/Carousel.astro';
---

<Carousel />
