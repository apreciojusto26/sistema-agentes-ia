---
// LEGACY PATH SHIM — kept deliberately, not dead code.
//
// The composition this file used to hold moved to
// @/design-system/blocks/product/HowItWorks/HorizontalTimeline.astro when
// product/HowItWorks gained real structural variants
// (vertical-steps | horizontal-timeline). The registry now points at the
// blocks; nothing resolves this path through the registry any more.
//
// It survives because design-system/test-fixtures/LegacyIndex2074c93.astro is
// BYTE-LOCKED to index.astro at HEAD 2074c93 and imports this module
// statically. legacy-render.golden.test.ts renders that fixture against the
// live default-DesignSpec page and requires the two to be byte-identical.
//
// Fifth capability with this arrangement, after 10-reviews-reel.astro,
// 04-gallery-strip.astro, 09-ugc-strip.astro and 08-faq.astro.
// Do not re-add markup here. Edit the block.
import HorizontalTimeline from '@/design-system/blocks/product/HowItWorks/HorizontalTimeline.astro';
---

<HorizontalTimeline />
