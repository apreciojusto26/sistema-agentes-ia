---
// LEGACY PATH SHIM — kept deliberately, not dead code.
//
// The composition this file used to hold moved to
// @/design-system/blocks/hero/Hero/Default.astro when hero/Hero gained real
// structural variants (default | split). The registry now points at the
// blocks; nothing resolves this path through the registry any more.
//
// It survives because design-system/test-fixtures/LegacyIndex2074c93.astro is
// BYTE-LOCKED and imports this module statically, and because
// test-fixtures/legacy-markup/historical-markup.golden.test.ts renders THIS
// path against the frozen 4732910 output.
//
// Seventh capability with this arrangement, after 10-reviews-reel.astro,
// 04-gallery-strip.astro, 09-ugc-strip.astro, 08-faq.astro,
// 06-how-it-works.astro and 11-comparison.astro.
// Do not re-add markup here. Edit the block.
import Default from '@/design-system/blocks/hero/Hero/Default.astro';
---

<Default />
