---
// LEGACY PATH SHIM — kept deliberately, not dead code.
//
// The composition this file used to hold moved to
// @/design-system/blocks/conversion/Guarantee/Default.astro when
// conversion/Guarantee absorbed conversion/ProductGuarantee. The registry now
// points at the block; nothing resolves this path through the registry any
// more.
//
// It survives because design-system/test-fixtures/LegacyIndex2074c93.astro is
// BYTE-LOCKED to index.astro at HEAD 2074c93 and imports this module
// statically. legacy-render.golden.test.ts renders that fixture against the
// live default-DesignSpec page and requires the two to be byte-identical.
//
// `tone="gold"` is passed EXPLICITLY even though it is also the block's
// default. Gold IS the legacy surface, and writing it down here means a later
// argument about which default reads friendlier cannot reach this render by
// accident.
// Do not re-add markup here. Edit the block.
import Guarantee from '@/design-system/blocks/conversion/Guarantee/Default.astro';
---

<Guarantee tone="gold" />
