node_modules/
.DS_Store
dist/
.astro/
.vercel/
.env
.env.*
!.env.example
scraper/output/
scraper/images/
outputs/*
!outputs/.gitkeep
admin/.jobs/
admin/.staged/

# pnpm local content-addressable store (regenerable, matches at any depth)
.pnpm-store/

# skill-registry cache — regenerated on demand, never source
.skill-registry.cache.json

# Operator merchant configuration — real legal identity (NIF, address,
# phone). It is the seller, not the code: it belongs to the machine that runs
# the Admin, never to the repository.
admin/merchant.json

# One file per generation run — reports the Admin reads back after a restart.
admin/.pipelines/
